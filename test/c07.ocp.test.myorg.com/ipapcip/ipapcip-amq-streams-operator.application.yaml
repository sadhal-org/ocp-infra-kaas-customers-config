apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ipapcip-amq-streams-operator
  namespace: skvinfra-argocd-applications
spec:
  project: kaas-customers
  source:
    repoURL: "oci://ghcr.io/sadhal"
    chart: "ipapcip-amq-streams-operator"
    targetRevision: "0.1.1"   # här styrs promotion; kaas-customer-team ändrar detta fält i config-repo
    helm:
      valueFiles:
        - ipapcip-amq-streams-operator.values.yaml
  destination:
    server: https://kubernetes.default.svc
    #namespace: acme
  syncPolicy:
    automated: {}   # för dev/test; för prod använd manual eller waves
